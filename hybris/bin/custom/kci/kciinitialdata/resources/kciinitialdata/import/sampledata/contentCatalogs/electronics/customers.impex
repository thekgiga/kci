$defaultPassword = 1234

INSERT_UPDATE UserGroup; UID[unique = true]
; orderemails ;

# Sample customer
INSERT_UPDATE Customer; originalUid[unique = true]; uid[unique = true]; title(code); name; description; sessionLanguage(isocode); sessionCurrency(isocode); groups(uid); password[default = $defaultPassword]; &userId
; test1@kci.com ; test1@kci.com ; mr ; kci test user with group ; kci test user with group ; en ; GBP ; customergroup, orderemails ; ; test1@kci.com
; test2@kci.com ; test2@kci.com ; mr ; kci test user with group ; kci test user with group ; en ; GBP ; customergroup              ; ; test2@kci.com